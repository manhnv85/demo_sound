import React, {Component} from 'react';
import{
    View,
    Text,
    Image,
    Dimensions
} from 'react-native';

class Header extends Component{
    render(){
        return(
            <View/>
        )
    }
}

export default Header;